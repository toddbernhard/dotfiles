# useful util
brew install bash
brew install tree
brew install the_silver_searcher

# pyenv dependencies, needed to build Python versions
brew install openssl
brew install readline
brew install sqlite3
brew install xz
brew install zlib
